version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend-app
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend-app/build
    files:
      - '**/*'
  cache:
    paths:
      - frontend-app/node_modules/**/*
